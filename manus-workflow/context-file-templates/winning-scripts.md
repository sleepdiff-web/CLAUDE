# Winning Scripts — [BUYER PROBLEM]

> This file TRAINS Manus. Rule: store the RAW winning script and NOTHING ELSE.
> No "winning hook" notes, no mechanism call-outs, no directions, no emphasis.
> The winner-harvest cron appends here automatically (Meta win-threshold → Notion script).
>
> Why raw-only: any analysis you add gets over-weighted and every new script clings to it.
> Keep it a plain bank. The bigger it grows, the more one-shots you get on short ads.

---

### [ad-name] — appended [date]
```
[full raw script, exactly as it ran]
```

---

### [ad-name] — appended [date]
```
[full raw script]
```
